#non
from datetime import datetime

if (dato == "2"):
    fecha_actual = datetime.now()
    print("La fecha actual es:", fecha_actual.strftime("%Y-%m-%d %H:%M:%S"))

if (dato == "3"):
    print("Cerrando programa...")
    exit()
